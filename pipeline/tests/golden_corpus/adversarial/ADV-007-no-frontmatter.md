# Article Without Any Frontmatter

This file has no YAML frontmatter block at all. There is no opening `---` delimiter. The parser should detect this immediately and return a structured error.

## Content

This is otherwise well-written content that would pass policy review if it had valid frontmatter.
